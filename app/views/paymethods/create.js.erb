$('#paymethods').html("<%= j (render @paymethods) %>");
$('#paymethod-form').slideUp(350);

$('#messages').html("<%= j (render 'layouts/messages') %>");

$('#pagination_top').html("<%= j (will_paginate(@paymethods, :params => { :controller => "paymethods", :action => "index" })) %>");
$('#pagination_bottom').html("<%= j (will_paginate(@paymethods, :params => { :controller => "paymethods", :action => "index" })) %>");
