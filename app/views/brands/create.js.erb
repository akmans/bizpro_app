$('#brands').html("<%= j (render @brands) %>");
$('#brand-form').slideUp(350);

$('#messages').html("<%= j (render 'layouts/messages') %>");

$('#pagination_top').html("<%= j (will_paginate(@brands, :params => { :controller => "brands", :action => "index" })) %>");
$('#total_entries').text("<%= "総件数：#{@brands.total_entries}件" %>");
$('#pagination_bottom').html("<%= j (will_paginate(@brands, :params => { :controller => "brands", :action => "index" })) %>");
