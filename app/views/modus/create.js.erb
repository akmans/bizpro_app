$('#modus').html("<%= j (render @modus) %>");
$('#modu-form').slideUp(350);

$('#messages').html("<%= j (render 'layouts/messages') %>");

$('#pagination_top').html("<%= j (will_paginate(@modus, :params => { :controller => "modus", :action => "index" })) %>");
$('#pagination_bottom').html("<%= j (will_paginate(@modus, :params => { :controller => "modus", :action => "index" })) %>");