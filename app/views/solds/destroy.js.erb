$('#solds').html("<%= j (render @solds) %>");

$('#messages').html("<%= j (render 'layouts/messages') %>");

$('#pagination_top').html("<%= j (will_paginate(@solds, :params => { :controller => "solds", :action => "index" })) %>");
$('#pagination_bottom').html("<%= j (will_paginate(@solds, :params => { :controller => "solds", :action => "index" })) %>");